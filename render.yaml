services:
  - type: web
    name: sentinel-osint-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web_dashboard:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
